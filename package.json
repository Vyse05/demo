{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "only-ui": "npx playwright test --ui --grep @only",
    "only":  "npx playwright test --grep @only",
    "api": "npx playwright test --grep @api",
    "ui": "npx playwright test --grep @ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.12.7"
  }
}
